# Runtime Assets

This folder is served directly as static files by Vite and should contain optimized runtime assets.

Examples:
- `.glb` models
- compressed textures
- audio clips
- UI sprites

Reference these in code via `/assets/...` URLs.
