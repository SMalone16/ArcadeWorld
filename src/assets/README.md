# Source Assets (Authoring)

Use this folder for source/working assets before optimization or conversion.

Examples:
- Blender source files
- Texture source files (e.g., PSD, Krita)
- Audio project files

Processed runtime-ready files should be copied to `public/assets/`.
